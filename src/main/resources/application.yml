allowed:
  origins: http://localhost:5173

spring:
  application:
    name: site
  datasource:
    url: jdbc:mysql://localhost:3306/creechy?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 6rB]dL74
    jooq:
      sql-dialect: MYSQL